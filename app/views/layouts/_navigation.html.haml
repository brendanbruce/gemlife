%nav.navigation
  %ul
    %li
      - if user_signed_in?
        %h2
          Signed in as
          = current_user.email
          = link_to 'Sign out', destroy_user_session_path, method: "delete"
      - else
        %h2 Not Signed In
    %li
      %a.test
        Test
    %li
      %a.test
        Test
